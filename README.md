# GaussianSplat

A Unity URP prototype for importing and rendering 3D Gaussian Splatting PLY data.

It parses Gaussian attributes into GPU buffers, sorts splats per camera on the CPU, and renders billboard splats with a custom shader.

Prototype project; not production-ready.
